{
  "name": "streamy",
  "version": "0.1.0",
  "description": "Streaming data formats in node and the browser",
  "main": "index.js",
  "scripts": {
    "test": "mocha",
    "bundle": "browserify index-browserify.js > bundle.js",
    "watch": "watchy -w index.js,index-browserify.js -- npm run bundle"
  },
  "author": "Shawn Allen <shawn.allen@hush.com>",
  "license": "ISC",
  "dependencies": {
    "JSONStream": "^0.10.0",
    "browserify": "^8.1.3",
    "csv-parser": "^1.5.0",
    "watchy": "^0.6.0"
  },
  "devDependencies": {
    "mocha": "^2.1.0"
  }
}
